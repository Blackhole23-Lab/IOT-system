{
  "name": "iot-system",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently -k -n SERVER,CLIENT -c cyan,magenta \"pnpm --filter server dev\" \"pnpm --filter client dev\"",
    "build": "pnpm --filter client build",
    "start": "pnpm --filter server start",
    "install:all": "pnpm install"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
